{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch mblue.test",
            "type": "dotnet",
            "request": "launch",
            "projectPath": "${workspaceFolder}/src/mblue.test.csproj"
        }
    ]
}
