{
  "name": "keekee-viewer-ui",
  "version": "1.0.0",
  "description": "KeeKee Viewer UI - TypeScript compilation",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rimraf dist/web/static/*.js dist/web/static/*.js.map"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "rimraf": "^5.0.5"
  }
}
